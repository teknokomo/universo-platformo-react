import gulp from 'gulp'

// Universo Platformo | Placeholder for future Gulp tasks
function placeholderTask(cb: () => void) {
    // This task does nothing for now.
    // You can add your build steps here in the future.
    cb()
}

// Universo Platformo | Default Gulp task
export default gulp.series(placeholderTask)
