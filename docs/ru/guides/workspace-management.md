---
description: Переключение рабочих пространств, создание общего рабочего пространства и управление участниками в приложениях с рабочими пространствами.
---

# Управление рабочими пространствами

Приложения с включёнными рабочими пространствами напрямую показывают рантайм-модель рабочих пространств в общем MUI shell.

## Что может делать пользователь

![Диалог управления рабочими пространствами](../.gitbook/assets/platform/applications-list.png)

- переключаться между доступными рабочими пространствами из навигационной панели приложения;
- создавать общее рабочее пространство;
- приглашать участников по `userId` и назначать рантайм-роль рабочего пространства;
- удалять участников из общего рабочего пространства;
- делать рабочее пространство текущим рабочим пространством по умолчанию.

## Почему это важно для LMS

LMS MVP использует рабочие пространства как границу совместной работы.
Например, одна команда преподавателей может работать в одном рабочем пространстве, а другая команда класса — в другом, и при этом рантайм-строки изолируются backend-предикатами с учётом рабочего пространства.

## Текущие роли

- `owner`
- `member`

Эти роли хранятся в схеме приложения и отделены от глобальных платформенных ролей.

## UI-компоненты

Текущая реализация использует:

- `WorkspaceSwitcher`
- `WorkspaceManagerDialog`

Оба компонента живут в `packages/apps-template-mui` и являются общими для любого приложения с включёнными рабочими пространствами.
