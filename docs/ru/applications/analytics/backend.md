# `packages/analytics-srv` — Backend системы аналитики — [Статус: Планируется]

> **📋 Уведомление**: Данная документация описывает планируемый backend компонент системы аналитики. Модуль находится в стадии проектирования.

## Назначение

Backend сервис для сбора, обработки и предоставления аналитических данных о производительности викторин, пользовательском взаимодействии и метриках вовлеченности.

## Роль в архитектуре

-   Агрегация и обработка данных из таблиц `leads` и `quiz_results`
-   RESTful API для получения аналитических метрик
-   Интеграция с базой данных Supabase через TypeORM
-   Безопасность на уровне строк (RLS) для изоляции данных

## Ключевые возможности (планируется)

### Управление данными

-   **Агрегация метрик**: Вычисление KPI и статистики в реальном времени
-   **Хранилище событий**: Сохранение событий пользовательского взаимодействия
-   **Исторические данные**: Архивирование и доступ к историческим метрикам
-   **Экспорт данных**: API для экспорта данных в различных форматах

### API конечные точки

#### Получение метрик викторины

```
GET /api/v1/analytics/quiz/:quizId/metrics
Query: {
  startDate: string,
  endDate: string,
  aggregation: 'day' | 'week' | 'month'
}

Response: {
  totalAttempts: number,
  completionRate: number,
  averageScore: number,
  timeSpent: number
}
```

#### Получение метрик пользователя

```
GET /api/v1/analytics/user/:userId/metrics
Query: {
  startDate: string,
  endDate: string
}

Response: {
  quizzesCompleted: number,
  averageScore: number,
  totalTimeSpent: number,
  lastActivity: timestamp
}
```

#### Экспорт данных

```
POST /api/v1/analytics/export
Body: {
  format: 'csv' | 'json' | 'xlsx',
  filters: {
    quizId?: string,
    userId?: string,
    dateRange: { start: string, end: string }
  }
}

Response: {
  downloadUrl: string,
  expiresAt: timestamp
}
```

## Интеграция

### База данных

-   **TypeORM**: Типобезопасные операции с базой данных
-   **Supabase**: PostgreSQL база с RLS политиками
-   **Миграции**: Управление схемой через TypeORM миграции

### Внешние сервисы

-   **flowise-server**: Интеграция с основным сервером
-   **auth-srv**: Аутентификация и авторизация пользователей
-   **Кэш**: Redis для кэширования агрегированных данных

## Безопасность

-   **RLS политики**: Пользователи видят только свои данные
-   **JWT токены**: Аутентификация на основе токенов
-   **Валидация входных данных**: Защита от SQL инъекций
-   **Rate limiting**: Защита от злоупотреблений API

## Разработка

### Настройка

```bash
# Установить зависимости
pnpm install

# Запустить в режиме разработки
pnpm --filter analytics-srv dev

# Собрать для продакшена
pnpm --filter analytics-srv build
```

### Тестирование

```bash
# Запустить unit тесты
pnpm --filter analytics-srv test

# Запустить integration тесты
pnpm --filter analytics-srv test:integration

# Линтинг
pnpm --filter analytics-srv lint
```

## Дорожная карта

### Фаза 1: Базовый функционал
- ⏳ Агрегация основных метрик (попытки, завершения, баллы)
- ⏳ RESTful API для чтения метрик
- ⏳ Интеграция с Supabase

### Фаза 2: Продвинутая аналитика
- ⏳ Когортный анализ
- ⏳ Воронки конверсии
- ⏳ A/B тестирование

### Фаза 3: Масштабирование
- ⏳ Кэширование с Redis
- ⏳ Оптимизация запросов
- ⏳ Поддержка больших объемов данных

## См. также

- [Analytics Frontend](./frontend.md) - Фронтенд компонент системы аналитики
- [Analytics README](./README.md) - Обзор системы аналитики
