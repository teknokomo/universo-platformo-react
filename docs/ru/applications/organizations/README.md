# Organizations (Организации)

> **📋 Уведомление**: Данная документация основана на оригинальной документации Flowise и в настоящее время адаптируется для Universo Platformo. Некоторые разделы могут все еще ссылаться на функциональность Flowise, которая еще не была полностью обновлена для специфичных возможностей Universo Platformo.

## Состав приложения

- **Frontend пакет**: `@universo/organizations-frt`
- **Backend пакет**: `@universo/organizations-srv`

## Обзор функционала

Приложение Organizations предоставляет комплексную систему управления организациями с трёхуровневой иерархической структурой. Модуль позволяет создавать организации, управлять департаментами внутри них и контролировать позиции (должности) в каждом департаменте с полной изоляцией данных между организациями.

### Архитектура: Organizations → Departments → Positions

```
┌─────────────────────────────────────┐
│ Organization (Организация)           │
│ ┌─────────────────────────────────┐ │
│ │ Department (Департамент)         │ │
│ │ ┌─────────────────────────────┐ │ │
│ │ │ Position (Позиция/Должность)│ │ │
│ │ └─────────────────────────────┘ │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

## Ключевые возможности

### 🏢 Управление организациями
- Создание, редактирование и удаление организаций
- Иерархическая структура с полной изоляцией данных
- Роль-основанный контроль доступа (RBAC)
- Управление членами организации с ролями

### 📊 Департаменты
- Логические группы внутри организации
- Привязка к конкретной организации
- Управление позициями внутри департамента

### 👤 Позиции (Должности)
- Индивидуальные должности в департаментах
- Обязательная принадлежность к департаменту (нет пустых опций)
- Метаданные для каждой позиции
- Полная изоляция между организациями

### 🎨 Пользовательский интерфейс
- Material-UI компоненты с современным дизайном
- Табличное и карточное представление данных
- Пагинация и поиск
- Диалоговые формы для CRUD операций
- Responsive дизайн для desktop и mobile

### 🌍 Интернационализация
- Полная поддержка английского и русского языков
- i18next интеграция
- Централизованные переводы в `@universo/i18n`

## Технические детали

### Frontend
- **[Подробнее о frontend пакете →](frontend.md)**
- React 18 + TypeScript + Material-UI
- React Query для кэширования данных
- Двойная сборка (CJS + ESM)

### Backend
- **[Подробнее о backend пакете →](backend.md)**
- Express.js + TypeORM
- PostgreSQL с миграциями
- RESTful API endpoints

## Интеграция с другими приложениями

### Зависимости
- **Workspaces (Uniks)**: Организации работают в контексте workspace
- **Authentication**: Passport.js + Supabase для аутентификации
- **Profile**: Связь с профилями пользователей через роли

### Используемые инфраструктурные пакеты
- `@universo/types` - Общие типы (OrganizationRole, validation schemas)
- `@universo/i18n` - Переводы (organizations namespace)
- `@universo/template-mui` - UI компоненты (ItemCard, tables, dialogs)
- `@universo/api-client` - HTTP клиент для API запросов

## Схема базы данных

### Таблицы

#### `organizations`
- `id` (uuid, PK)
- `name` (text, NOT NULL)
- `description` (text)
- `createdDate` (timestamp)
- `updatedDate` (timestamp)

#### `organizations_users` (связь many-to-many)
- `id` (uuid, PK)
- `organizationId` (uuid, FK)
- `userId` (uuid, FK)
- `role` (OrganizationRole: 'owner' | 'admin' | 'editor' | 'member')
- `@Unique(['organizationId', 'userId'])`

#### `departments`
- `id` (uuid, PK)
- `name` (text, NOT NULL)
- `description` (text)
- `organizationId` (uuid, FK) - обязательная связь

#### `departments_organizations` (связь many-to-many)
- `id` (uuid, PK)
- `departmentId` (uuid, FK)
- `organizationId` (uuid, FK)
- `@Unique(['departmentId', 'organizationId'])`

#### `positions`
- `id` (uuid, PK)
- `name` (text, NOT NULL)
- `description` (text)
- `metadata` (jsonb) - дополнительные поля
- `departmentId` (uuid, FK) - обязательная связь

#### `positions_departments` (связь many-to-many)
- `id` (uuid, PK)
- `positionId` (uuid, FK)
- `departmentId` (uuid, FK)
- `@Unique(['positionId', 'departmentId'])`

#### `positions_organizations` (связь many-to-many)
- `id` (uuid, PK)
- `positionId` (uuid, FK)
- `organizationId` (uuid, FK)
- `@Unique(['positionId', 'organizationId'])`

## Примеры использования

### Создание организации через API
```typescript
POST /api/v1/organizations
{
  "name": "Acme Corp",
  "description": "Основная организация компании"
}
```

### Добавление департамента
```typescript
POST /api/v1/organizations/:orgId/departments
{
  "name": "IT Department",
  "description": "Информационные технологии"
}
```

### Создание позиции в департаменте
```typescript
POST /api/v1/organizations/:orgId/departments/:deptId/positions
{
  "name": "Senior Developer",
  "description": "Опытный разработчик",
  "metadata": {
    "level": "senior",
    "experience": "5+ years"
  }
}
```

## Безопасность

### Изоляция данных
- Полная изоляция позиций и департаментов между организациями
- Никакой cross-organization видимости
- Frontend и backend валидация для предотвращения утечек данных

### Контроль доступа
- Роль-основанные права доступа (Owner, Admin, Editor, Member)
- TypeORM middleware для проверки membership
- Row-Level Security (RLS) политики как дополнительный слой

### Иерархия ролей
- **Owner** (4): Полный контроль над организацией
- **Admin** (3): Управление членами и структурой
- **Editor** (2): Редактирование контента
- **Member** (1): Просмотр данных

## Статус разработки

- ✅ Backend entities (Organizations, Departments, Positions)
- ✅ TypeORM migrations (PostgreSQL)
- ✅ RESTful API endpoints
- ✅ Frontend components (List, Board, Forms)
- ✅ i18n (EN, RU)
- ✅ Integration с flowise-ui
- ✅ Двойная сборка (CJS + ESM)
- ✅ Vitest тесты

**Дата запуска**: Q4 2024
**Текущая версия**: 0.1.0

## Следующие шаги

- Изучите [Frontend пакет](frontend.md) для понимания UI компонентов
- Изучите [Backend пакет](backend.md) для понимания API и базы данных
- См. [Главную страницу приложений](../README.md) для понимания общей архитектуры
