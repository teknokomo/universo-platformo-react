/**
 * Re-export from canonical location in @universo/template-mui.
 * Kept for backward compatibility — existing domain imports continue to work.
 */
export { applyOptimisticReorder, rollbackReorderSnapshots, reorderItemsBySortOrder } from '@universo/template-mui'
export type { ReorderQuerySnapshot } from '@universo/template-mui'
