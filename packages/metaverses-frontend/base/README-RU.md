# @universo/metaverses-frontend

> 🏗️ **Современный пакет** - TypeScript-first архитектура с двойной системой сборки

## Обзор

Фронтенд-приложение для управления мета-вселенными, разделами и сущностями в экосистеме Universo Platformo. Metaverses Frontend предоставляет комплексные UI-процессы для управления трёхуровневой архитектурой Мета-вселенные → Разделы → Сущности с полной изоляцией данных и безопасностью.

## Информация о пакете

- **Пакет**: `@universo/metaverses-frontend`
- **Версия**: `0.1.0`
- **Тип**: React Frontend Package (Modern)
- **Фреймворк**: React 18 + TypeScript + Material-UI
- **Система сборки**: tsdown (двойная сборка - CJS + ESM)
- **Тестирование**: Vitest + React Testing Library

## Ключевые функции

### 🌍 Управление мета-вселенными
- **Иерархическая организация**: Трёхуровневая архитектура (Мета-вселенные → Разделы → Сущности)
- **Полная изоляция данных**: Сущности и разделы из разных мета-вселенных полностью разделены
- **Ролевой доступ**: Пользовательские роли и разрешения для контроля доступа к мета-вселенным
- **Контекстная навигация**: Навигация с учётом мета-вселенной с хлебными крошками и сохранением сайдбара

### 🎨 Пользовательский интерфейс
- **Интеграция Material-UI**: Согласованные UI-компоненты с современной системой дизайна
- **Адаптивный дизайн**: Оптимизирован для настольных и мобильных устройств
- **Табличное и карточное представление**: Гибкое отображение данных с пагинацией и поиском
- **Диалоговые формы**: Модальные формы для создания и редактирования сущностей

### 🔧 Технические функции
- **TypeScript-First**: Полная реализация на TypeScript со строгой типизацией
- **Интеграция React Query**: Продвинутая загрузка данных и кэширование
- **Интернационализация**: Английские и русские переводы с i18next
- **Валидация форм**: Комплексная валидация с обработкой ошибок
- **Интеграция API**: RESTful API клиент с аутентификацией

## Установка и настройка

### Предварительные требования
```bash
# Системные требования
Node.js >= 18.0.0
PNPM >= 8.0.0
```

### Установка
```bash
# Установить зависимости
pnpm install

# Собрать пакет
pnpm --filter @universo/metaverses-frontend build

# Запустить в режиме разработки
pnpm --filter @universo/metaverses-frontend dev
```

### Интеграция
```tsx
// Импорт компонентов в ваше React-приложение
import { MetaverseList, MetaverseBoard, metaversesDashboard } from '@universo/metaverses-frontend'

// Импорт i18n ресурсов
import { metaversesTranslations } from '@universo/metaverses-frontend'

// Использование в маршрутах
<Route path="/metaverses" element={<MetaverseList />} />
<Route path="/metaverses/:id/board" element={<MetaverseBoard />} />
```

## Архитектура

### Трёхуровневая модель сущностей
- **Мета-вселенные**: Организационные единицы верхнего уровня, обеспечивающие полную изоляцию данных
- **Разделы**: Логические группировки внутри мета-вселенных (например, "Веб-сервисы", "Мобильные приложения")
- **Сущности**: Отдельные активы, принадлежащие конкретным разделам внутри мета-вселенных

### Стратегия изоляции данных
- Полное разделение между мета-вселенными - отсутствие видимости между ними
- Все операции поддерживают контекст мета-вселенной через URL-маршрутизацию
- Фронтенд и бэкенд валидация предотвращают создание сиротских сущностей
- Ролевой контроль доступа для разрешений мета-вселенных

## Использование

### Базовые компоненты
```tsx
import { MetaverseList, MetaverseBoard } from '@universo/metaverses-frontend'

// Список мета-вселенных с возможностями управления
function MetaversesPage() {
  return <MetaverseList />
}

// Панель управления и аналитика мета-вселенной
function MetaverseBoardPage() {
  return <MetaverseBoard />
}
```

### Интеграция API
```tsx
import { useApi } from '@universo/metaverses-frontend/hooks'
import * as metaversesApi from '@universo/metaverses-frontend/api'

function MetaverseData() {
  const { data: metaverses, isLoading } = useApi(
    metaversesApi.getMetaverses
  )
  
  if (isLoading) return <div>Загрузка...</div>
  return <div>Найдено {metaverses?.length} мета-вселенных</div>
}
```

### Интеграция меню
```tsx
import { metaversesDashboard } from '@universo/metaverses-frontend'

// Добавить в навигационное меню
const menuItems = [
  ...otherMenuItems,
  metaversesDashboard
]
```

## Структура файлов

```
packages/metaverses-frontend/base/
├── src/
│   ├── api/              # Функции API клиента
│   │   ├── metaverses.ts   # CRUD операции мета-вселенных
│   │   ├── sections.ts     # Управление разделами
│   │   ├── entities.ts     # Операции с сущностями
│   │   └── queryKeys.ts    # Ключи React Query
│   ├── hooks/            # Пользовательские React хуки
│   │   ├── useApi.ts       # Хук интеграции API
│   │   └── index.ts        # Экспорт хуков
│   ├── i18n/             # Интернационализация
│   │   ├── locales/        # Языковые файлы (en, ru)
│   │   │   ├── en.json     # Английские переводы
│   │   │   └── ru.json     # Русские переводы
│   │   └── index.ts        # Конфигурация i18n
│   ├── pages/            # Основные компоненты страниц
│   │   ├── MetaverseList.tsx   # Основной компонент списка
│   │   ├── MetaverseBoard.tsx  # Компонент панели управления
│   │   └── MetaverseActions.ts # Определения действий
│   ├── menu-items/       # Конфигурация навигации
│   │   └── metaverseDashboard.ts
│   ├── types/            # Определения TypeScript
│   │   ├── index.ts        # Основные экспорты типов
│   │   └── types.ts        # Определения типов
│   ├── utils/            # Вспомогательные функции
│   └── index.ts          # Экспорты пакета
├── dist/                 # Скомпилированный вывод (CJS, ESM, types)
├── package.json
├── tsconfig.json
├── tsdown.config.ts      # Конфигурация сборки
├── vitest.config.ts      # Конфигурация тестов
├── README.md             # Английская документация
└── README-RU.md          # Данный файл
```

## Основные компоненты

### MetaverseList
Основной компонент для отображения и управления мета-вселенными:

```tsx
import { MetaverseList } from '@universo/metaverses-frontend'

// Функции:
// - Пагинированный табличный вид с функцией поиска
// - Операции создания, редактирования, удаления
// - Ролевой контроль доступа
// - Отзывчивый дизайн с Material-UI
// - Поддержка интернационализации
```

### MetaverseBoard  
Компонент панели управления для аналитики мета-вселенных:

```tsx
import { MetaverseBoard } from '@universo/metaverses-frontend'

// Функции:
// - Специфичная для мета-вселенной панель управления
// - Аналитика и статистика
// - Интерактивная визуализация данных
// - Контекстно-зависимая навигация
```

## Интеграция API

### Базовые операции API
```typescript
import * as metaversesApi from '@universo/metaverses-frontend/api'

// Получить все мета-вселенные
const metaverses = await metaversesApi.getMetaverses()

// Получить конкретную мета-вселенную
const metaverse = await metaversesApi.getMetaverse(id)

// Создать новую мета-вселенную
const newMetaverse = await metaversesApi.createMetaverse({
  name: 'Моя мета-вселенная',
  description: 'Описание мета-вселенной'
})

// Обновить мета-вселенную
const updated = await metaversesApi.updateMetaverse(id, data)

// Удалить мета-вселенную
await metaversesApi.deleteMetaverse(id)
```

### Операции в контексте мета-вселенной
```typescript
// Получить разделы для конкретной мета-вселенной
const sections = await metaversesApi.getMetaverseSections(metaverseId)

// Получить сущности для конкретной мета-вселенной  
const entities = await metaversesApi.getMetaverseEntities(metaverseId)

// Связать раздел с мета-вселенной
await metaversesApi.addSectionToMetaverse(metaverseId, sectionId)
```

### Интеграция React Query
```typescript
import { useQuery } from '@tanstack/react-query'
import { metaversesQueryKeys } from '@universo/metaverses-frontend/api'

function useMetaverses() {
  return useQuery({
    queryKey: metaversesQueryKeys.all,
    queryFn: metaversesApi.getMetaverses
  })
}
```

## Разработка

### Предварительные требования
- Node.js 18+
- pnpm 8+
- TypeScript 5+

### Доступные скрипты
```bash
# Разработка
pnpm dev              # Запуск сервера разработки
pnpm build            # Сборка для продакшена (двойная CJS/ESM)
pnpm build:watch      # Сборка в режиме наблюдения

# Тестирование
pnpm test             # Запуск тестового пакета Vitest
pnpm test:watch       # Запуск тестов в режиме наблюдения
pnpm test:coverage    # Генерация отчёта покрытия

# Качество кода
pnpm lint             # Запуск ESLint
pnpm lint:fix         # Исправление ошибок ESLint
pnpm type-check       # Проверка типов TypeScript
```

### Система сборки
Данный пакет использует `tsdown` для двойного вывода сборки:
- **CommonJS**: `dist/index.js` (для совместимости с устаревшими системами)
- **ES Modules**: `dist/index.mjs` (для современных сборщиков)
- **Типы**: `dist/index.d.ts` (объявления TypeScript)

### Рекомендации по разработке

#### Архитектурные паттерны
- **Трёхуровневая модель**: Мета-вселенные → Разделы → Сущности
- **Изоляция данных**: Строгие границы контекста между мета-вселенными
- **React Query**: Централизованная загрузка и кэширование данных
- **Material-UI**: Согласованное использование библиотеки компонентов

#### Управление контекстом
```typescript
// Всегда поддерживать контекст мета-вселенной
const metaverseContext = useMetaverseContext()
const sections = useSections(metaverseContext.id)
```

#### Валидация форм
```typescript
// Валидация обязательных полей
const entitySchema = z.object({
  name: z.string().min(1),
  sectionId: z.string().min(1), // Обязательно - нет пустой опции
  description: z.string().optional()
})
```

## Тестирование

### Структура тестов
```
src/
├── __tests__/
│   ├── components/
│   ├── hooks/
│   ├── api/
│   └── utils/
└── vitest.config.ts
```

### Подход к тестированию
```typescript
// Тестирование компонентов с React Testing Library
import { render, screen } from '@testing-library/react'
import { MetaverseList } from '../MetaverseList'

test('отображает список мета-вселенных', () => {
  render(<MetaverseList />)
  expect(screen.getByText('Мета-вселенные')).toBeInTheDocument()
})
```

### Запуск тестов
```bash
pnpm test                    # Запуск всех тестов
pnpm test:watch              # Режим наблюдения
pnpm test:coverage           # С покрытием
pnpm test -- --reporter=verbose  # Подробный вывод
```

## Конфигурация

### Переменные окружения
```bash
# Конфигурация API
VITE_API_URL=http://localhost:3000
VITE_API_VERSION=v1

# Аутентификация
VITE_AUTH_ENABLED=true
VITE_AUTH_PROVIDER=supabase
```

### Конфигурация TypeScript
Пакет использует строгую конфигурацию TypeScript:
- Включены строгие проверки null
- Нет неявных типов any
- Строгие типы функций
- Включены все строгие опции компилятора

## Участие в разработке

### Стиль кода
- Следуйте конфигурации ESLint
- Используйте Prettier для форматирования
- Предпочитайте TypeScript вместо JavaScript
- Используйте функциональные компоненты с хуками

### Процесс Pull Request
1. Создайте feature ветку от `main`
2. Реализуйте изменения с тестами
3. Обновите документацию
4. Запустите полный тестовый пакет
5. Отправьте PR с описанием

### Соглашение о коммитах
Следуйте соглашению conventional commits:
```bash
feat(metaverses): добавить функцию поиска
fix(api): обработка пустого ответа
docs(readme): обновить руководство по установке
```

## Связанные пакеты
- [`@universo/metaverses-backend`](../metaverses-backend/base/README.md) - Бэкенд сервис
- [`@universo/template-mui`](../universo-template-mui/base/README.md) - UI компоненты
- [`@universo/types`](../universo-types/base/README.md) - Общие типы

---
*Часть [Universo Platformo](../../../README-RU.md) - Комплексная платформа управления мета-вселенными*
