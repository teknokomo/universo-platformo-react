# @universo/flowise-tools-srv

Бэкенд-пакет для домена Tools в Universo Platformo (извлечён из flowise-server).

## Обзор

Этот пакет предоставляет бэкенд-функциональность для управления пользовательскими инструментами, которые могут использоваться в AI-воркфлоу. Инструменты позволяют интегрировать внешние API и пользовательские функции в chatflow и agentflow.

## Информация о пакете

- **Пакет**: `@universo/flowise-tools-srv`
- **Версия**: `0.1.0`
- **Тип**: Бэкенд (Современный)
- **Фреймворк**: Express.js, TypeORM, TypeScript
- **Зависимости**: `@universo/uniks-srv` (связь с сущностью Unik)

## Ключевые возможности

### 🗄️ Слой базы данных
- **Сущность Tool**: TypeORM-сущность со связью с Unik для мультитенантной изоляции
- **Миграции**: Миграции PostgreSQL для схемы таблицы инструментов

### 🔧 Сервисный слой
- **ToolsService**: CRUD-операции с паттерном DI
- **Валидация**: Zod-схемы для операций создания/обновления
- **Обработка ошибок**: Кастомные классы ошибок с корректными HTTP-статусами

### 🛣️ Слой маршрутов
- **Express Router**: RESTful API-эндпоинты с middleware обработки ошибок
- **Паттерн DI**: Фабричные функции для внедрения зависимостей

## Установка

```bash
pnpm add @universo/flowise-tools-srv
```

## Использование

```typescript
import {
    createToolsService,
    createToolsRouter,
    Tool,
    toolsMigrations
} from '@universo/flowise-tools-srv'

// Создание сервиса с DI
const toolsService = createToolsService({
    getDataSource: () => dataSource,
    telemetry: telemetryService,
    metrics: metricsProvider
})

// Создание роутера
const toolsRouter = createToolsRouter({ toolsService })

// Подключение роутера
app.use('/api/v1/unik/:unikId/tools', toolsRouter)
```

## Экспорты

### База данных
- `Tool` - TypeORM-сущность
- `toolsMigrations` - Массив миграций

### Сервисы
- `createToolsService` - Фабричная функция сервиса
- `IToolsService` - Интерфейс сервиса
- `ToolsServiceConfig` - Тип конфигурации
- `ToolsServiceError` - Кастомный класс ошибки
- `createToolSchema`, `updateToolSchema` - Zod-схемы валидации

### Маршруты
- `createToolsRouter` - Фабричная функция роутера
- `toolsErrorHandler` - Express middleware обработки ошибок
- `ToolsControllerError` - Класс ошибки контроллера

## Структура файлов

```
packages/flowise-tools-srv/
├── base/
│   ├── src/
│   │   ├── database/
│   │   │   ├── entities/      # Сущность Tool
│   │   │   └── migrations/    # Миграции PostgreSQL
│   │   ├── services/          # Бизнес-логика
│   │   ├── routes/            # Express-роутер
│   │   └── index.ts           # Точка входа
│   ├── dist/                  # Скомпилированный код
│   ├── package.json
│   ├── tsconfig.json
│   ├── README.md              # Документация (EN)
│   └── README-RU.md           # Этот файл
└── package.json               # Конфигурация воркспейса
```

## API-эндпоинты

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/unik/:unikId/tools` | Получить все инструменты для Unik |
| GET | `/unik/:unikId/tools/:id` | Получить конкретный инструмент по ID |
| POST | `/unik/:unikId/tools` | Создать новый инструмент |
| PUT | `/unik/:unikId/tools/:id` | Обновить существующий инструмент |
| DELETE | `/unik/:unikId/tools/:id` | Удалить инструмент |

## Разработка

```bash
# Установка зависимостей
pnpm install

# Сборка
pnpm build

# Запуск линтера
pnpm lint

# Режим отслеживания
pnpm dev
```

## Лицензия

Apache License Version 2.0 - См. файл [LICENSE](../../../LICENSE) для подробностей.
