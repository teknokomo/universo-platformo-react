// Minimal stub for canvases API used during package build
const canvasesApi = {
    async getCanvas(id) {
        return { data: null }
    },
    async updateCanvas(id, payload) {
        return { data: null }
    }
}

export default canvasesApi
