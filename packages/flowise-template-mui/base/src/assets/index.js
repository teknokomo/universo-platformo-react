// Assets re-exports
// Images and SCSS are imported directly by consumers
// They should not be bundled, only referenced

// This module exists for consistency but doesn't export anything bundleable
// Actual assets are copied via postbuild script
export default {}
