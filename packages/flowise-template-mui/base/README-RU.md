# Flowise Template MUI

🚨 **ПРЕДУПРЕЖДЕНИЕ О LEGACY КОДЕ** 🚨  
Этот пакет является частью устаревшей архитектуры Flowise и запланирован к удалению/рефакторингу после завершения миграции Universo Platformo (планируется на Q2 2026). Новые функции следует разрабатывать в современных пакетах `@universo/*`.

## Обзор

Библиотека компонентов Material-UI, извлеченная из Flowise UI, предоставляющая полный набор переиспользуемых UI компонентов, макетов, тем и утилит для создания приложений на основе Flowise. Этот пакет использует "unbundled source pattern", где компоненты потребляются напрямую из исходных файлов, а не из собранных артефактов.

## Информация о пакете

- **Пакет**: `@flowise/template-mui`
- **Версия**: `0.1.0` (legacy версия)
- **Тип**: UI Component Library (Legacy)
- **Фреймворк**: React + Material-UI 5.15.0 + Emotion
- **Паттерн**: Unbundled source компоненты (без шага сборки)
- **Зависимости**: Material-UI, Framer Motion, CodeMirror, workspace пакеты

## Ключевые особенности

### 🎨 Библиотека UI компонентов
- **50+ компонентов**: Полный набор компонентов на основе Material-UI
- **Система диалогов**: 20+ специализированных диалоговых компонентов для различных случаев
- **Элементы форм**: Компоненты input, dropdown, checkbox, slider и switch
- **Отображение данных**: Таблицы, карточки, сетки и компоненты визуализации данных
- **Навигация**: Компоненты макета, защита маршрутов и системы меню

### 🎭 Система тем
- **Темный/Светлый режимы**: Полная поддержка переключения тем
- **Интеграция Material-UI**: Пользовательские конфигурации тем с Material-UI
- **SCSS переменные**: Централизованные переменные цветов и стилей
- **Переопределения компонентов**: Согласованные стили для всех компонентов

### 🔧 Системы утилит
- **Пользовательские хуки**: API вызовы, уведомления, подтверждения и управление состоянием
- **Error Boundaries**: Компоненты границ ошибок React
- **Состояния загрузки**: Backdrop loaders, skeleton loading и компоненты переходов
- **Редактор кода**: Интеграция CodeMirror с подсветкой синтаксиса

### 🏗️ Архитектурный паттерн
- **Source-Only пакет**: Без шага сборки, компоненты потребляются напрямую из источника
- **Гранулированные экспорты**: Детализированные пути экспорта для оптимального tree shaking
- **Интеграция Workspace**: Использует современные пакеты `@universo/*` для API и i18n

## Установка и настройка

### Предварительные требования
```bash
# Peer dependencies
React ^18.3.1
React DOM ^18.3.1
```

### Установка
```bash
# Установка в workspace
pnpm install @flowise/template-mui

# Сборка не нужна - source-only пакет
```

### Базовая настройка
```jsx
import { ThemeProvider } from '@mui/material/styles'
import { theme } from '@flowise/template-mui/themes'
import { MainCard, AnimateButton } from '@flowise/template-mui'

function App() {
  const customization = { isDarkMode: false, borderRadius: 12 }
  
  return (
    <ThemeProvider theme={theme(customization)}>
      <MainCard title="Пример">
        <AnimateButton>Нажми меня</AnimateButton>
      </MainCard>
    </ThemeProvider>
  )
}
```

## Категории компонентов

### Компоненты кнопок
```jsx
import { 
  AnimateButton, 
  CopyToClipboardButton, 
  StyledButton,
  ThumbsUpButton,
  ThumbsDownButton 
} from '@flowise/template-mui'
```

### Компоненты диалогов
```jsx
import { 
  ConfirmDialog, 
  SaveCanvasDialog, 
  AddEditCredentialDialog,
  ExpandTextDialog,
  NodeInfoDialog 
} from '@flowise/template-mui'
```

### Компоненты форм
```jsx
import { 
  Input, 
  Dropdown, 
  MultiDropdown, 
  CheckboxInput, 
  SwitchInput,
  InputSlider 
} from '@flowise/template-mui'
```

### Компоненты макета
```jsx
import { 
  MainLayout, 
  MinimalLayout, 
  ViewHeaderMUI,
  AuthGuard 
} from '@flowise/template-mui'
```

### Компоненты отображения данных
```jsx
import { 
  DataGrid, 
  FlowListTable, 
  MainCard, 
  ItemCard,
  StatsCard 
} from '@flowise/template-mui'
```

## Пользовательские хуки

### API хук
```jsx
import { useApi } from '@flowise/template-mui/hooks'

function MyComponent() {
  const api = useApi()
  
  const fetchData = async () => {
    const result = await api.get('/api/chatflows')
    return result.data
  }
}
```

### Хук уведомлений
```jsx
import { useNotifier } from '@flowise/template-mui/hooks'

function MyComponent() {
  const { enqueueSnackbar } = useNotifier()
  
  const showSuccess = () => {
    enqueueSnackbar({
      message: 'Успех!',
      options: { variant: 'success' }
    })
  }
}
```

### Хук подтверждения
```jsx
import { useConfirm } from '@flowise/template-mui/hooks'

function MyComponent() {
  const { confirm } = useConfirm()
  
  const handleDelete = async () => {
    const result = await confirm({
      title: 'Удалить элемент',
      description: 'Вы уверены?'
    })
    if (result) {
      // Логика удаления
    }
  }
}
```

## Темизация

### Конфигурация темы
```jsx
import { theme } from '@flowise/template-mui/themes'

const customization = {
  isDarkMode: true,
  borderRadius: 8,
  fontFamily: 'Roboto',
  isOpen: false,
  opened: true
}

const customTheme = theme(customization)
```

### Пользовательские цвета темы
```scss
// Доступ к SCSS п��ременным
@import '~@flowise/template-mui/src/assets/scss/_themes-vars.module.scss';

.custom-component {
  background-color: $primary200;
  color: $grey900;
}
```

## Архитектура

### Структура экспортов
```javascript
// Гранулированные экспо��ты для tree shaking
'./hooks': './src/hooks/index.ts'
'./layout': './src/layout/index.js'
'./themes': './src/themes/index.js'
'./ui-components/*': './src/ui-components/*'
'./utils/*': './src/utils/*'
```

### Организация компонентов
```
src/
├── ui-components/          # Основная библиотека компонентов
│   ├── button/            # Варианты кнопок
│   ├── dialog/            # Модальные диалоги
│   ├── cards/             # Компоненты карточек
│   ├── input/             # Элементы форм
│   ├── table/             # Таблицы данных
│   ├── loading/           # Состояния загрузки
│   └── ...
├── layout/                # Компоненты макета
├── themes/                # Система тем
├── hooks/                 # Пользовательские хуки
├── assets/                # Изображения, иконки, стили
└── types/                 # Определения TypeScript
```

## Структура файлов

```
packages/flowise-template-mui/
├── base/                   # Реализация пакета
│   ├── src/
│   │   ├── ui-components/  # Основная библиотека компонентов (15+ категорий)
│   │   │   ├── button/     # Компоненты кнопок
│   │   │   ├── dialog/     # 20+ компонентов диалогов
│   │   │   ├── cards/      # Компоненты карточек
│   │   │   ├── input/      # Элементы форм
│   │   │   ├── table/      # Таблицы данных
│   │   │   ├── loading/    # Компоненты загрузки
│   │   │   ├── markdown/   # Рендеринг Markdown
│   │   │   ├── editor/     # Редактор кода
│   │   │   └── ...         # И другие категории
│   │   ├── layout/         # Компоненты макета
│   │   │   ├── MainLayout/ # Основной макет приложения
│   │   │   └── MinimalLayout/ # Минимальный макет
│   │   ├── themes/         # Система тем
│   │   │   ├── palette.js  # Цветовые палитры
│   │   │   ├── typography.js # Настройки типографии
│   │   │   └── compStyleOverride.js # Переопределения компонентов
│   │   ├── hooks/          # Пользовательские хуки
│   │   │   ├── useApi.js   # API хук
│   │   │   ├── useNotifier.js # Хук уведомлений
│   │   │   └── useConfirm.js  # Хук подтверждения
│   │   ├── assets/         # Статические ресурсы
│   │   │   ├── images/     # Изображения
│   │   │   └── scss/       # SCSS переменные тем
│   │   ├── routes/         # Компоненты маршрутов
│   │   ├── menu-items/     # Конфигурация меню
│   │   ├── config.js       # Конфигурация
│   │   ├── constants.ts    # Константы
│   │   ├── ErrorBoundary.jsx # Граница ошибок
│   │   └── index.ts        # Основные экспорты
│   ├── package.json
│   ├── tsconfig.json
│   └── LICENSE-Flowise.md
└── README.md              # Этот файл
```

## Статус Legacy и план миграции

### Текущее состояние (2024)
- ✅ **Функциональная**: Полная библиотека компонентов Material-UI
- ✅ **Интегрированная**: Используется `flowise-ui` и связанными legacy компонентами
- ⚠️ **Заморожена**: Никаких новых компонентов, только режим поддержки
- ⚠️ **Unbundled**: Использует source-only паттерн, который может вызывать проблемы

### График миграции
- **Q1 2025**: Оценка современных альтернатив библиотек компонентов
- **Q2 2025**: Начало миграции к современным решениям компонентов
- **Q3 2025**: Создание современной библиотеки компонентов в пакете `@universo/ui`
- **Q4 2025**: Прекращение использования legacy template
- **Q1 2026**: Завершение миграции к современной библиотеке компонентов
- **Q2 2026**: Удаление legacy пакета template

### Стратегия замены
1. **Современная библиотека компонентов**: Замена на современную систему компонентов
2. **Система дизайна**: Реализация согласованных токенов дизайна и системы
3. **TypeScript First**: Полная реализация TypeScript
4. **Оптимизация сборки**: Правильная система сборки с tree shaking
5. **Производительность**: Оптимизация размера бандла и производительности runtime

## Зависимости

### Основные зависимости
```json
{
  "@emotion/react": "^11.11.4",
  "@emotion/styled": "^11.11.5",
  "@mui/material": "5.15.0",
  "@mui/icons-material": "^5.0.3",
  "@mui/x-data-grid": "^5.17.26",
  "framer-motion": "^10.16.4"
}
```

### Зависимости Workspace
```json
{
  "@flowise/store": "workspace:*",
  "@universo/api-client": "workspace:*",
  "@universo/i18n": "workspace:*"
}
```

### Зависимости редактора кода
```json
{
  "@uiw/react-codemirror": "^4.23.0",
  "@uiw/codemirror-theme-sublime": "^4.23.0",
  "@uiw/codemirror-theme-vscode": "^4.23.0"
}
```

## Разработка

### Локальная разработка
```bash
# Установка зависимостей
pnpm install

# Проверка типов
pnpm typecheck

# Линтинг
pnpm lint

# Сборка не нужна (source-only пакет)
echo 'No build needed for source-only package'
```

### Добавление новых функций (Legacy пакет)
⚠️ **Важно**: Это legacy код. Новые UI компоненты следует разрабатывать в современных пакетах `@universo/*` когда это возможно.

Если вы должны добавить компоненты в этот legacy пакет:
1. Следуйте существующим паттернам Material-UI
2. Поддерживайте обратную совместимость
3. Добавьте соответствующие определения TypeScript
4. Следуйте существующей структуре экспортов
5. Документируйте путь миграции для новых компонентов

## Точки интеграции

### Интеграция Legacy пакетов
- **flowise-ui**: Основной потребитель всех компонентов
- **flowise-chatmessage**: Использует диалоги и компоненты карточек
- **flowise-store**: Интегрируется с хуками и управлением состоянием

### Интеграция современных пакетов
- **@universo/api-client**: Используется API хуками
- **@universo/i18n**: Используется для интернационализации
- **@universo/types**: Определения типов (если доступно)

## Известные ограничения

1. **Unbundled Source**: Компоненты потребляются напрямую из исходных файлов
2. **Смешанные расширения файлов**: Несогласованное использование .js/.jsx/.ts/.tsx
3. **Большой бандл**: Нет правильного tree shaking из-за source паттерна
4. **Версия Material-UI**: Заблокирована на конкретной версии MUI
5. **Сложность сборки**: Source паттерн может вызывать проблемы сборки

## Руководство по миграции

### От Legacy компонентов
```jsx
// Старый паттерн (legacy)
import { MainCard } from '@flowise/template-mui'

// Новый паттерн (будущий)
import { Card } from '@universo/ui'
```

### Сопоставление компонентов
- `MainCard` → `@universo/ui/Card`
- `AnimateButton` → `@universo/ui/Button`
- `ConfirmDialog` → `@universo/ui/Dialog`
- `Input` → `@universo/ui/TextField`

## Внесение вклада

⚠️ **Уведомление о Legacy пакете**: Этот пакет находится в режиме поддержки. Для новых вкладов:
1. Рассмотрите реализацию UI компонентов в современных пакетах `@universo/*`
2. Следуйте существующим паттернам Material-UI, если изменения необходимы
3. Поддерживайте обратную совместимость с существующими потребителями
4. Добавьте правильные определения TypeScript для новых компонентов
5. Документируйте путь миграции для любых новых UI элементов

## Лицензия

SEE LICENSE IN LICENSE-Flowise.md - Apache License Version 2.0

---

**Поддержка миграции**: Если вам нужна помощь в миграции UI компонентов из этого legacy пакета к современным альтернативам, пожалуйста, обратитесь к документации по миграции или создайте issue для получения руководства.