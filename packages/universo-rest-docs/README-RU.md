# @universo/rest-docs

> 📚 Документация REST API для Universo Platformo

## Информация о пакете

| Поле | Значение |
|------|----------|
| **Имя пакета** | `@universo/rest-docs` |
| **Версия** | Смотрите `package.json` |
| **Тип** | Node.js API документация |
| **Сборка** | TypeScript с Swagger/OpenAPI |
| **Назначение** | Интерактивная документация для публичных API Flowise |

## 🚀 Ключевые возможности

- 📚 **Полная документация API** - Список всех публичных API Flowise
- 🔧 **Программный доступ** - Выполнение задач программно, как в GUI
- 📖 **Интерактивная документация** - Swagger UI для тестирования API
- 🎯 **Автономный режим** - Может работать отдельно от основного сервера
- 🔄 **Режим разработки** - Автоматическая перезагрузка при изменениях

## Описание

Комплексная документация REST API для экосистемы Universo Platformo. Этот пакет предоставляет интерфейс Swagger UI, который документирует все публичные API, позволяя пользователям программно выполнять те же задачи, что доступны в графическом интерфейсе.

Документация автоматически генерируется из спецификаций OpenAPI/Swagger и включает:
- Эндпоинты аутентификации
- Управление чатфлоу
- Операции с рабочими пространствами
- API конструктора пространств
- Рабочие процессы публикации
- Управление профилями

## Обзор API

### Основные эндпоинты

| Категория эндпоинтов | Базовый путь | Описание |
|---------------------|--------------|----------|
| **Аутентификация** | `/api/auth` | Вход, регистрация, управление сессией |
| **Чатфлоу** | `/api/chatflows` | CRUD операции с AI-чатфлоу |
| **Рабочие пространства** | `/api/uniks` | Управление и совместная работа в workspace |
| **Пространства** | `/api/spaces` | Управление 3D/AR/VR пространствами |
| **Публикация** | `/api/publish` | Экспорт рабочих процессов на различные платформы |
| **Профили** | `/api/profiles` | Профиль пользователя и настройки |

### Примеры вызовов API

#### Список всех чатфлоу
```bash
curl -X GET "http://localhost:3000/api/chatflows" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

#### Создание нового рабочего пространства
```bash
curl -X POST "http://localhost:3000/api/uniks" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name": "Мое рабочее пространство", "description": "Проектное пространство"}'
```

#### Экспорт пространства в PlayCanvas
```bash
curl -X POST "http://localhost:3000/api/publish/playcanvas" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"spaceId": "space-uuid", "target": "production"}'
```

## Использование

### Автономный режим

## Запуск в автономном режиме

1. Запустите сервер Flowise:
    ```sh
    cd Flowise
    pnpm start
    ```
2. Запустите сервер документации API:
    ```sh
    cd packages/universo-rest-docs
    pnpm start
    ```

### Режим разработки

## Запуск в режиме разработки

```sh
cd Flowise
pnpm dev
```

### Доступ к документации

После запуска интерактивный Swagger UI будет доступен по адресу:
- **Локально**: `http://localhost:6655/api-docs`
- **Продакшн**: `https://your-domain.com/api-docs`

## Возможности

### Интерактивное тестирование
- **Try It Out**: Выполнение вызовов API прямо из документации
- **Аутентификация**: Тестирование эндпоинтов с API ключами или сессионными токенами
- **Валидация ответов**: Просмотр ответов и кодов статуса в реальном времени
- **Инспекция схем**: Детальное изучение моделей запросов/ответов

### Генерация кода
Swagger UI предоставляет сниппеты кода на нескольких языках:
- cURL
- JavaScript/Node.js
- Python
- Java
- C#

### Версионирование API
Документация версионируется для отслеживания изменений:
- **v1**: Начальный публичный API
- **v2**: Расширенная совместная работа в workspace (текущая)

## Конфигурация

### Переменные окружения

```env
# Порт сервера документации API
REST_DOCS_PORT=6655

# URL основного сервера Flowise (для автономного режима)
FLOWISE_SERVER_URL=http://localhost:3000

# Путь к спецификации OpenAPI
OPENAPI_SPEC_PATH=./openapi.yaml
```

## Интеграция

Этот пакет интегрируется с:
- **Flowise Server**: Монтирует документацию на эндпоинт `/api-docs`
- **Система аутентификации**: Валидирует сессионные токены для защищённых эндпоинтов
- **TypeScript Types**: Общие определения типов из `@universo/types`

## Разработка

### Обновление документации API

При добавлении новых эндпоинтов:
1. Обновите спецификацию OpenAPI в `openapi.yaml`
2. Добавьте JSDoc аннотации к обработчикам маршрутов
3. Пересоберите документацию: `pnpm build`
4. Проверьте изменения в Swagger UI

### Тестирование

```bash
pnpm --filter universo-rest-docs test
```

## Лицензия

Исходный код в этом репозитории доступен под лицензией Apache License Version 2.0.