# Flowise Server

🚨 **ПРЕДУПРЕЖДЕНИЕ О LEGACY КОДЕ** 🚨  
Этот пакет является частью устаревшей архитектуры Flowise и запланирован к удалению/рефакторингу после завершения миграции Universo Platformo (планируется на Q2 2026). Новые функции следует разрабатывать в современных пакетах `@universo/*`.

## Обзор

Основной backend сервер для Universo Platformo, предоставляющий базовый REST API на Express, управление базой данных и сервисы аутентификации. Этот пакет служит центральным узлом для всего функционала Flowise и интегрируется с современными пакетами сервисов `@universo/*`.

## Информация о пакете

- **Пакет**: `flowise`
- **Версия**: `2.2.8` (замороженная legacy версия)
- **Тип**: Backend Server (Legacy)
- **Фреймворк**: Express.js + TypeORM + OCLIF CLI
- **База данных**: PostgreSQL через Supabase
- **Аутентификация**: Passport.js + JWT + интеграция с Supabase

## Ключевые особенности

### 🎯 Основной функционал сервера
- **Express API Server**: Полный REST API с ограничением скорости и middleware
- **Управление базой данных**: Интеграция TypeORM с PostgreSQL и Supabase
- **Система аутентификации**: Сессии Passport.js с проверкой JWT токенов
- **Реестр сущностей**: Централизованное управление сущностями и миграциями TypeORM
- **Управление очередями**: Очереди задач на основе Redis для фоновой обработки
- **Метрики и телеметрия**: Интеграция с Prometheus и OpenTelemetry

### 🔐 Функции безопасности
- **CSRF защита**: Защита от CSRF атак с использованием токенов `csurf`
- **Ограничение скорости**: Express rate limiting с Redis backend
- **XSS защита**: Middleware для очистки запросов
- **Управление сессиями**: Безопасные сессии на основе cookies
- **Валидация API ключей**: Многоуровневая аутентификация с fallback на API ключи

### 🏗️ Интеграция архитектуры
- **Интеграция современных сервисов**: Импортирует и использует пакеты `@universo/*`
- **Система узлов Flowise**: Интеграция с legacy `flowise-components` и `flowise-ui`
- **Поддержка мультиплеера**: Интеграция с Colyseus multiplayer сервером
- **Система Canvas**: Интеграция с `@universo/spaces-srv` для выполнения потоков

## CLI команды (OCLIF)

### Основные команды
```bash
# Запуск сервера
pnpm start

# Запуск в режиме разработки
pnpm dev

# Список всех доступных команд
pnpm flowise --help

# Управление пользователями
pnpm user                              # Список всех пользователей
pnpm user --email admin@example.com --password newpass  # Сброс пароля
```

### Переменные окружения
```bash
# Основная конфигурация
PORT=3000
HOST=localhost
NODE_ENV=production

# База данных (Supabase)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_JWT_SECRET=your-jwt-secret

# Управление сессиями
SESSION_SECRET=your-strong-secret
SESSION_COOKIE_NAME=up.session
SESSION_COOKIE_MAXAGE=86400000
SESSION_COOKIE_SAMESITE=lax
SESSION_COOKIE_SECURE=false

# Ограничение скорости
REDIS_URL=redis://localhost:6379

# Обработка файлов
FLOWISE_FILE_SIZE_LIMIT=50mb

# Метрики (опционально)
ENABLE_METRICS=true
METRICS_PROVIDER=prometheus
```

## Компоненты архитектуры

### Основной сервер (`src/index.ts`)
- **Класс App**: Центральное управление приложением
- **Инициализация базы данных**: Настройка TypeORM и миграции
- **Стек middleware**: Аутентификация, CORS, ограничение скорости, управление сессиями
- **Регистрация маршрутов**: API v1 маршруты и endpoints аутентификации

### Система аутентификации
- **Проверка JWT**: Валидация Supabase JWT токенов
- **Управление сессиями**: Passport.js + express-session
- **Многоуровневая авторизация**: Токены сессий → Bearer токены → API ключи
- **CSRF защита**: Защита от межсайтовых подделок запросов

### Слой базы данных
- **Реестр сущностей**: Центральная регистрация в `src/database/entities/index.ts`
- **Реестр миграций**: PostgreSQL миграции в `src/database/migrations/postgres/index.ts`
- **Интеграция TypeORM**: Полная реализация паттерна репозитория
- **Подключение Supabase**: Прямое подключение к PostgreSQL через Supabase

### CLI фреймворк (OCLIF)
- **Директория команд**: `src/commands/` содержит все реализации CLI
- **Базовая команда**: Общий функционал в `src/commands/base.ts`
- **Команда запуска**: Основной запуск сервера в `src/commands/start.ts`
- **Управление пользователями**: CRUD операции для аутентификации

## Зависимости

### Основные зависимости фреймворка
```json
{
  "express": "^4.18.2",
  "@oclif/core": "^3.18.1",
  "typeorm": "^0.3.20",
  "passport": "^0.7.0",
  "express-session": "^1.17.3"
}
```

### Зависимости интеграции Universo
```json
{
  "@universo/auth-srv": "workspace:*",
  "@universo/spaces-srv": "workspace:*",
  "@universo/metaverses-srv": "workspace:*",
  "@universo/multiplayer-colyseus-srv": "workspace:*",
  "@universo/utils": "workspace:*"
}
```

### AI и LangChain зависимости
```json
{
  "langchain": "~0.1.25",
  "@langchain/core": "~0.1.52",
  "llamaindex": "^0.1.12",
  "chromadb": "^1.8.1"
}
```

## Структура файлов

```
packages/flowise-server/
├── bin/                    # OCLIF CLI исполняемые файлы
│   ├── run                 # Основная точка входа CLI
│   ├── run.cmd            # Windows CLI wrapper
│   ├── dev                # Точка входа для разработки
│   └── dev.cmd            # Windows dev wrapper
├── src/
│   ├── commands/          # Реализации команд OCLIF
│   │   ├── base.ts        # Базовый класс команд
│   │   ├── start.ts       # Команда запуска сервера
│   │   └── user.ts        # Команды управления пользователями
│   ├── database/          # Слой базы данных
│   │   ├── entities/      # Реестр сущностей TypeORM
│   │   └── migrations/    # Реестр миграций
│   ├── middlewares/       # Express middleware
│   ├── routes/           # Определения API маршрутов
│   ├── utils/            # Утилиты
│   ├── metrics/          # Провайдеры метрик
│   ├── queue/            # Управление очередями
│   ├── DataSource.ts     # Источник данных TypeORM
│   ├── Interface.ts      # Определения типов
│   └── index.ts          # Основное приложение сервера
├── cypress/              # E2E тестирование с Cypress
├── .env.example         # Шаблон переменных окружения
├── package.json         # Конфигурация пакета
└── README.md           # Этот файл
```

## Статус Legacy и план миграции

### Текущее состояние (2024)
- ✅ **Функциональный**: Полностью работающий legacy сервер
- ✅ **Интегрированный**: Успешно интегрирован с современными пакетами `@universo/*`
- ✅ **Защищенный**: Обновленная система аутентификации с интеграцией Supabase
- ⚠️ **Заморожен**: Версия заблокирована на 2.2.8, никаких новых крупных функций

### График миграции
- **Q1 2025**: Продолжение модернизации пакетов `@universo/*`
- **Q2 2025**: Начало постепенной миграции функционала сервера
- **Q3 2025**: Предупреждения об устаревании и документация по миграции
- **Q4 2025**: Заморозка функций и подготовка к миграции
- **Q1 2026**: Начало рефакторинга сервера
- **Q2 2026**: Завершение миграции и удаление legacy кода

### Стратегия замены
1. **API слой**: Миграция к современной настройке Express в новом пакете `@universo/server`
2. **Слой базы данных**: Сохранение паттернов TypeORM с модернизацией управления сущностями
3. **Аутентификация**: Сохранение интеграции Supabase с упрощением middleware стека
4. **CLI инструменты**: Миграция основных команд в новую структуру пакетов
5. **Система очередей**: Модернизация обработки фоновых задач

## Тестирование

### E2E тестирование с Cypress
```bash
# Установка Cypress
./node_modules/.bin/cypress install

# Сначала сборка проекта
pnpm build

# Запуск E2E тестов
pnpm run e2e

# Открытие GUI Cypress (только для разработки)
pnpm run cypress:open
```

### Структура тестов
- **Тесты Cypress**: Расположены в директории `cypress/`
- **Файлы поддержки**: Пользовательские команды и утилиты в `cypress/support/`
- **Интеграционные тесты**: Полное тестирование потоков для API endpoints

## Разработка

### Локальная разработка
```bash
# Установка зависимостей
pnpm install

# Запуск в режиме разработки
pnpm dev

# Сборка проекта
pnpm build

# Запуск продакшн сервера
pnpm start
```

### Добавление новых функций (Legacy пакет)
⚠️ **Важно**: Это legacy код. Новые функции следует разрабатывать в современных пакетах `@universo/*` когда это возможно.

Если вы должны добавить функции в этот legacy пакет:
1. Следуйте существующим паттернам и архитектуре
2. Обеспечьте совместимость с современными пакетами
3. Добавьте соответствующие уведомления об устаревании
4. Документируйте путь миграции для функции

## Ограничение скорости и продакшн развертывание

Для продакшн развертываний с ограничением скорости на основе Redis, обратитесь к подробному руководству:

**[Руководство по развертыванию с ограничением скорости](../universo-utils/base/DEPLOYMENT.md)**

Это руководство охватывает:
- Конфигурацию Redis и настройку подключения (`REDIS_URL`)
- Примеры развертывания Docker, Kubernetes и PM2
- Проверки здоровья и мониторинг
- Устранение распространенных проблем

## Точки интеграции

### Интеграция современных пакетов
- **Аутентификация**: Использует `@universo/auth-srv` для конфигурации Passport.js
- **Пространства**: Интегрируется с `@universo/spaces-srv` для выполнения canvas потоков
- **Метавселенные**: Использует `@universo/metaverses-srv` для инициализации rate limiter
- **Мультиплеер**: Координируется с `@universo/multiplayer-colyseus-srv`
- **Утилиты**: Использует `@universo/utils` для сетевых функций и ограничения скорости

### Зависимости Legacy пакетов
- **UI**: Обслуживает статические файлы из пакета `flowise-ui`
- **Компоненты**: Интегрируется с системой узлов `flowise-components`
- **Шаблоны**: Использует `flowise-template-mui` для UI компонентов

## Документация

- **API документация**: См. `/docs/ru/api-reference/` для деталей REST API
- **Руководства по развертыванию**: Проверьте `/docs/ru/getting-started/` для инструкций по настройке
- **Руководства по миграции**: Обратитесь к `/docs/ru/migration-guide/` для обновлений версий

## Внесение вклада

⚠️ **Уведомление о Legacy пакете**: Этот пакет находится в режиме поддержки. Для новых вкладов:
1. Рассмотрите, принадлежит ли функция современному пакету `@universo/*`
2. Следуйте существующим паттернам кода, если изменения необходимы
3. Добавьте соответствующие тесты для любых модификаций
4. Документируйте путь миграции для новых функций

## Лицензия

Apache License Version 2.0 - См. файл [LICENSE](../../LICENSE) для деталей.

---

**Поддержка миграции**: Если вам нужна помощь в миграции функций из этого legacy пакета к современным альтернативам, пожалуйста, обратитесь к документации по миграции или создайте issue для получения руководства.