# Flowise UI

🚨 **ПРЕДУПРЕЖДЕНИЕ О LEGACY КОДЕ** 🚨  
Этот пакет является частью устаревшей архитектуры Flowise и запланирован к удалению/рефакторингу после завершения миграции Universo Platformo (планируется на Q2 2026). Новые функции следует разрабатывать в современных пакетах `@universo/*`.

## Обзор

Основное React frontend приложение для интерфейса визуального программирования Universo Platformo, предоставляющее полный веб-интерфейс для создания, управления и развертывания AI рабочих потоков. Это основной пользовательский интерфейс, который интегрирует все функции Flowise с современными возможностями Universo.

## Информация о пакете

- **Пакет**: `flowise-ui`
- **Версия**: `2.2.8` (замороженная legacy версия)
- **Тип**: React Frontend Application (Legacy)
- **Фреймворк**: React 18 + Vite + Material-UI + React Flow
- **Паттерн**: Основное приложение, которое потребляет все остальные пакеты
- **Система сборки**: Vite с выделением 8GB памяти

## Ключевые особенности

### 🎨 Интерфейс визуального программирования
- **React Flow Canvas**: Визуальное программирование на основе узлов с drag-and-drop
- **200+ AI компонентов узлов**: Полная библиотека узлов LangChain и AI интеграций
- **Выполнение в реальном времени**: Живое выполнение рабочих потоков с потоковыми результатами
- **Система шаблонов**: Переиспользуемые шаблоны рабочих потоков и интеграция с marketplace

### 🏗️ Архитектура приложения
- **Мульти-представленческий интерфейс**: Редактор canvas, тестирование чатбота, управление учетными данными
- **Интеграция компонентов**: Использует `@flowise/template-mui` для всех UI компонентов
- **Управление состоянием**: Интеграция Redux через `@flowise/store`
- **Интеграция современных пакетов**: Включает все пакеты `@universo/*`

### 🌐 Функции платформы Universo
- **Аутентификация**: Современная система авторизации через `@universo/auth-frt`
- **Пространства**: Управление рабочими областями через `@universo/spaces-frt`
- **Публикация**: Публикация контента через `@universo/publish-frt`
- **Метавселенные**: Интеграция виртуальных миров через `@universo/metaverses-frt`
- **Конструктор пространств**: Создание 3D пространств через `@universo/space-builder-frt`
- **Шаблоны**: Шаблоны викторин и интерактивных элементов

### 🔧 Функции разработки
- **Горячая перезагрузка**: Сервер разработки на базе Vite
- **Поддержка TypeScript**: Смешанный JS/TS с постепенной миграцией
- **Интернационализация**: Многоязычная поддержка через `@universo/i18n`
- **Интеграция API**: Централизованный API клиент через `@universo/api-client`

## Установка и настройка

### Предварительные требования
```bash
# Системные требования
Node.js >= 18.0.0
PNPM >= 8.0.0
8GB+ RAM (для процесса сборки)
```

### Настройка разработки
```bash
# Установка зависимостей
pnpm install

# Запуск сервера разработки
pnpm dev

# Сборка для продакшн
pnpm build

# Очистка артефактов сборки
pnpm clean
```

### Конфигурация окружения
```bash
# Копирование примера окружения
cp .env.example .env.local

# Настройка переменных окружения
VITE_PORT=3000
VITE_API_BASE_URL=http://localhost:3001
# Добавьте другие настройки по необходимости
```

## Архитектура

### Структура приложения
```
src/
├── views/                  # Основные представления приложения
│   ├── canvas/            # Редактор визуальных потоков
│   ├── chatbot/           # Интерфейс чата
│   ├── credentials/       # Управление API учетными данными
│   ├── agentflows/        # Рабочие потоки AI агентов
│   ├── publish/           # Интерфейс публикации
│   ├── up-auth/           # Представления аутентификации
│   ├── up-admin/          # Административный интерфейс
│   └── ...
├── components/            # Общие компоненты
├── api/                   # Слой интеграции API
├── config/                # Конфигурация
├── shims/                 # Shims совместимости
└── App.jsx                # Основной компонент приложения
```

### Интеграция пакетов
- **UI компоненты**: `@flowise/template-mui` - Полная библиотека Material-UI
- **Управление состоянием**: `@flowise/store` - Конфигурация Redux store
- **Компоненты узлов**: `flowise-components` - Узлы AI рабочих потоков
- **Современные функции**: Все пакеты `@universo/*` для новой функциональности

### Конфигурация сборки
```javascript
// vite.config.js - Оптимизировано для крупномасштабного приложения
{
  build: {
    rollupOptions: {
      output: {
        manualChunks: { /* оптимизированное разделение */ }
      }
    }
  }
}
```

## Представления и функции

### Редактор Canvas (`/canvas`)
- Редактор визуальных рабочих потоков с React Flow
- Размещение узлов drag-and-drop
- Валидация соединений в реальном времени
- Выполнение и тестирование рабочих потоков

### Интерфейс чатбота (`/chatbot`)
- Интерактивное тестирование чата
- История разговоров
- Система обратной связи по сообщениям
- Обработка потоковых ответов

### Управление учетными данными (`/credentials`)
- Управление API ключами
- Аутентификация сервисов
- Безопасное хранение учетных данных
- Тестирование интеграции

### Платформа публикации (`/publish`)
- Создание и управление контентом
- Рабочий поток публикации
- Интеграция системы шаблонов

### Административный интерфейс (`/up-admin`)
- Управление пользователями
- Конфигурация системы
- Аналитика и мониторинг

## Интеграция API

### Паттерн useApi Hook
```javascript
import { useApi } from '@flowise/template-mui/hooks'

function MyComponent() {
  const { request } = useApi(fetchList)
  
  useEffect(() => {
    request()
  }, [request])
}
```

### Интеграция API клиента
```javascript
import { useApiClient } from '@universo/api-client'

function DataComponent() {
  const api = useApiClient()
  
  const fetchData = async () => {
    const response = await api.get('/api/chatflows')
    return response.data
  }
}
```

## Структура файлов

```
packages/flowise-ui/
├── public/                 # Статические ресурсы
├── src/
│   ├── views/             # Представления приложения (15+ модулей)
│   │   ├── canvas/        # Редактор визуальных потоков
│   │   ├── chatbot/       # Интерфейс чата
│   │   ├── credentials/   # Управление учетными данными
│   │   ├── agentflows/    # Рабочие потоки агентов
│   │   ├── assistants/    # AI ассистенты
│   │   ├── docstore/      # Хранилище документов
│   │   ├── marketplaces/  # Marketplace шаблонов
│   │   ├── publish/       # Платформа публикации
│   │   ├── settings/      # Настройки приложения
│   │   ├── tools/         # Управление инструментами
│   │   ├── up-admin/      # Административный интерфейс
│   │   ├── up-auth/       # Аутентификация
│   │   └── variables/     # Управление переменными
│   ├── components/        # Общие компоненты
│   │   └── BootstrapErrorBoundary.jsx
│   ├── api/              # Интеграция API
│   ├── config/           # Конфигурация
│   ├── diagnostics/      # Утилиты отладки
│   ├── shims/            # Слои совместимости
│   ├── App.jsx           # Основное приложение
│   └── index.jsx         # Точка входа приложения
├── .env.example          # Шаблон окружения
├── vite.config.js        # Конфигурация Vite
├── package.json
├── README.md             # Этот файл
└── LICENSE-Flowise.md
```

## Статус Legacy и план миграции

### Текущее состояние (2024)
- ✅ **Функциональное**: Полное рабочее React приложение
- ✅ **Интегрированное**: Успешно включает все современные пакеты `@universo/*`
- ✅ **Поддерживаемое**: Активная разработка с интеграцией современных функций
- ⚠️ **Смешанная архитектура**: Объединяет legacy Flowise с современными функциями Universo

### График миграции
- **Q1 2025**: Завершение интеграции оставшихся пакетов `@universo/*`
- **Q2 2025**: Начало постепенной миграции от legacy компонентов Flowise
- **Q3 2025**: Создание новой современной структуры UI приложения
- **Q4 2025**: Прекращение использования legacy представлений и компонентов
- **Q1 2026**: Запуск нового современного UI приложения
- **Q2 2026**: Завершение миграции и отказ от legacy UI

### Стратегия замены
1. **Современный UI фреймворк**: Новое React приложение с современной архитектурой
2. **Миграция компонентов**: Постепенная замена legacy компонентов
3. **Модернизация представлений**: Перестройка представлений с современной системой дизайна
4. **Оптимизация производительности**: Оптимизация размера бандла и производительности runtime
5. **Developer Experience**: Улучшение рабочего процесса разработки и инструментов

## Зависимости

### Основные React зависимости
```json
{
  "react": "^18.3.1",
  "react-dom": "^18.3.1",
  "react-router-dom": "~6.3.0",
  "react-redux": "^8.0.5"
}
```

### Зависимости Material-UI
```json
{
  "@mui/material": "5.15.0",
  "@mui/icons-material": "5.0.3",
  "@mui/x-data-grid": "6.8.0"
}
```

### Современные пакеты Universo
```json
{
  "@universo/auth-frt": "workspace:*",
  "@universo/api-client": "workspace:*",
  "@universo/i18n": "workspace:*",
  "@universo/spaces-frt": "workspace:*",
  "@universo/publish-frt": "workspace:*"
}
```

### Legacy пакеты Flowise
```json
{
  "@flowise/store": "workspace:*",
  "@flowise/template-mui": "workspace:*"
}
```

## Разработка

### Сервер разработки
```bash
# Запуск сервера разработки
pnpm dev

# Сервер запустится на http://localhost:3000
# Включена горячая перезагрузка
```

### Процесс сборки
```bash
# Продакшн сборка (требует 8GB RAM)
NODE_OPTIONS='--max-old-space-size=8192' pnpm build

# Вывод сборки в директорию build/
# Оптимизировано для продакшн развертывания
```

### Добавление новых функций
⚠️ **Рекомендации по разработке**:
1. **Предпочитайте современные пакеты**: Используйте пакеты `@universo/*` для новых функций
2. **Переиспользование компонентов**: Используйте компоненты `@flowise/template-mui` когда возможно
3. **Управление состоянием**: Используйте Redux store через `@flowise/store`
4. **Интеграция API**: Используйте `@universo/api-client` для связи с backend
5. **Интернационализация**: Поддерживайте несколько языков через `@universo/i18n`

## Точки интеграции

### Интеграция современных пакетов
- **Аутентификация**: `@universo/auth-frt` для потоков входа/выхода
- **API клиент**: `@universo/api-client` для связи с backend
- **Интернационализация**: `@universo/i18n` для многоязычной поддержки
- **Пространства**: `@universo/spaces-frt` для управления рабочими областями
- **Публикация**: `@universo/publish-frt` для создания контента

### Зависимости Legacy пакетов
- **UI библиотека**: `@flowise/template-mui` для всех UI компонентов
- **Store состояний**: `@flowise/store` для управления состоянием Redux
- **Система узлов**: Прямая интеграция с `flowise-components`

## Соображения производительности

### Оптимизация сборки
- **Выделение памяти**: 8GB размера heap для сложных сборок
- **Разделение кода**: Оптимизация chunks на базе Vite
- **Анализ бандла**: Регулярный мониторинг размера бандла
- **Tree Shaking**: Оптимизированные импорты для меньших бандлов

### Производительность Runtime
- **React Query**: Кэширование и синхронизация данных
- **Виртуализация**: Обработка больших списков в data grids
- **Ленивая загрузка**: Разделение кода на основе маршрутов
- **Мемоизация**: Оптимизация компонентов и вычислений

## Известные ограничения

1. **Большой размер бандла**: Сложное приложение с множеством зависимостей
2. **Смешанная архитектура**: Сосуществование legacy и современного кода
3. **Требования к памяти**: Высокое использование памяти во время сборки
4. **Сложность компонентов**: Глубокая иерархия компонентов
5. **Legacy зависимости**: Зависимость от legacy пакетов Flowise

## Внесение вклада

🔄 **Активная разработка**: Этот пакет активно разрабатывается, но с планированием миграции:
1. **Новые функции**: Должны интегрировать современные пакеты `@universo/*`
2. **Legacy код**: Поддерживать совместимость с существующей функциональностью
3. **Подготовка к миграции**: Документировать и планировать будущую миграцию
4. **Производительность**: Мониторить и оптимизировать производительность приложения
5. **Современные паттерны**: Использовать современные React паттерны для нового кода

## Лицензия

SEE LICENSE IN LICENSE-Flowise.md - Apache License Version 2.0

---

**Поддержка миграции**: Это UI приложение является основной точкой интеграции для всей платформы. Планирование миграции критично для поддержания функциональности при модернизации архитектуры.