# @universo/metapanel-frontend

Shared authenticated landing dashboard for the current Universo Platformo React shell.

## Overview

This package provides the metapanel entry view that surfaces high-level platform statistics for authenticated users after onboarding.

## Development

```bash
pnpm --filter @universo/metapanel-frontend build
pnpm --filter @universo/metapanel-frontend lint
```
