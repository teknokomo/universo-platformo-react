# Optional frontend-only overrides for browser e2e runs.
# The production e2e flow uses the backend static bundle served by `pnpm start`,
# so these values are only used when a local frontend build needs explicit overrides.

# VITE_HOST=127.0.0.1
# VITE_PORT=8080
