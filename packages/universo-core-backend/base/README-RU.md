# @universo/core-backend

> 🗄️ Основная инфраструктура базы данных и миграции для Universo Platformo

## Информация о пакете

| Поле | Значение |
|------|----------|
| **Название пакета** | `@universo/core-backend` |
| **Версия** | См. `package.json` |
| **Тип** | TypeScript Backend пакет |
| **Сборка** | CommonJS с TypeScript определениями |
| **Назначение** | Основная инфраструктура базы данных и важные миграции |

## 🚀 Ключевые возможности

- 🆔 **Функция UUID v7** - Генерация временно-упорядоченных UUID согласно RFC 9562 для PostgreSQL
- 🏗️ **Инфраструктурные миграции** - Функции базы данных, требуемые всеми модулями
- ⚡ **Оптимизация производительности** - Индексирование на 30-50% быстрее чем UUID v4 из-за последовательности
- 🔧 **Готовность к PostgreSQL 17+** - Совместимость с будущей нативной функцией uuidv7()
- 📦 **Минимальные зависимости** - Легковесный пакет только с основной инфраструктурой

## Описание

Пакет основной инфраструктуры базы данных, содержащий важные миграции и функции, требуемые всей системой Universo Platformo. Этот пакет должен загружаться ПЕРВЫМ в последовательности миграций для предоставления функций уровня базы данных, используемых всеми другими модулями.

### Область применения:
- Функция генерации UUID v7 (соответствует RFC 9562)
- Миграции основной инфраструктуры базы данных
- Важные функции, требуемые всеми модулями

### Вне области применения:
- Миграции специфичные для приложений
- Схемы базы данных для конкретных функций
- Бизнес-логика или реализации сервисов

## Порядок миграций

⚠️ **КРИТИЧЕСКИ ВАЖНО**: Этот пакет содержит миграции Фазы 0 инфраструктуры, которые ДОЛЖНЫ выполняться первыми:

```typescript
import { infrastructureMigrations } from '@universo/core-backend'

export const postgresMigrations = [
    // Фаза 0: Инфраструктура (ДОЛЖНА БЫТЬ ПЕРВОЙ)
    ...infrastructureMigrations,
    // Фаза 1+: Другие миграции...
    ...otherMigrations
]
```

## Реализация UUID v7

Пакет предоставляет функцию `public.uuid_generate_v7()`, которая:
- Следует спецификации RFC 9562 (май 2024)
- Использует 48-битную временную метку Unix в миллисекундах для временного упорядочивания
- Обеспечивает лучшую локальность индексов чем UUID v4
- Совместима с нативной реализацией PostgreSQL 18+
- Работает с текущим PostgreSQL 17.x (Supabase)

## Использование

### Импорт миграций:

```typescript
import { infrastructureMigrations } from '@universo/core-backend'

// Добавить в ваш реестр миграций
export const postgresMigrations = [
    ...infrastructureMigrations,  // Должно быть первым!
    ...yourOtherMigrations
]
```

### Прямой доступ к миграции:

```typescript
import { InitializeUuidV7Function1500000000000 } from '@universo/core-backend'
```

## Установка

Этот пакет автоматически включается через pnpm workspace:

```bash
pnpm add @universo/core-backend
```

## Зависимости

- `typeorm` (peer зависимость)
- Использует функции специфичные для PostgreSQL
- Нет дополнительных runtime зависимостей

## Совместимость

- ✅ **PostgreSQL 17.x** (Supabase)
- ✅ **PostgreSQL 18+** (прямая совместимость)
- ⚠️ **Только PostgreSQL** (не поддерживает другие базы данных)

## Заметки о производительности

UUID v7 обеспечивает значительные преимущества производительности:
- **На 30-50% быстрее индексирование** по сравнению со случайным UUID v4
- **Лучшая локальность кэша** из-за последовательной природы
- **Сниженная фрагментация индексов** для столбцов первичного ключа
- **Временно-упорядоченная сортировка** без дополнительных столбцов временных меток

## Безопасность миграций

Все миграции в этом пакете спроектированы как:
- ✅ **Идемпотентные** - Безопасны для многократного выполнения
- ✅ **Неразрушающие** - Только создают функции, никогда не удаляют данные
- ✅ **Обратно совместимые** - Функции работают на всех версиях PostgreSQL

## Лицензия

См. `LICENSE-Flowise.md` в корне проекта.