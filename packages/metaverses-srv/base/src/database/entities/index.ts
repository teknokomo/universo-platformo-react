import { Metaverse } from './Metaverse'
import { MetaverseUser } from './MetaverseUser'
import { Section } from './Section'
import { SectionMetaverse } from './SectionMetaverse'
import { Entity } from './Entity'
import { EntityMetaverse } from './EntityMetaverse'
import { EntitySection } from './EntitySection'
import { AuthUser } from './AuthUser'
import { Profile } from '@universo/profile-srv'

export const metaversesEntities = [
    Metaverse,
    MetaverseUser,
    Section,
    SectionMetaverse,
    Entity,
    EntityMetaverse,
    EntitySection,
    AuthUser,
    Profile
]

export { Metaverse, MetaverseUser, Section, SectionMetaverse, Entity, EntityMetaverse, EntitySection, AuthUser, Profile }
