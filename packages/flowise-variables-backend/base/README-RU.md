# @universo/flowise-variables-backend

Бэкенд-пакет для домена Variables в Universo Platformo (извлечён из flowise-server).

## Обзор

Этот пакет предоставляет бэкенд-функциональность для управления переменными, которые могут использоваться в AI-воркфлоу. Переменные позволяют хранить переиспользуемые значения, такие как промпты, конфигурации и динамические данные, на которые можно ссылаться в chatflow и agentflow.

## Информация о пакете

- **Пакет**: `@universo/flowise-variables-backend`
- **Версия**: `0.1.0`
- **Тип**: Бэкенд (Современный)
- **Фреймворк**: Express.js, TypeORM, TypeScript
- **Зависимости**: `@universo/uniks-backend` (связь с сущностью Unik)

## Ключевые возможности

### 🗄️ Слой базы данных
- **Сущность Variable**: TypeORM-сущность со связью с Unik для мультитенантной изоляции
- **Миграции**: Миграции PostgreSQL для схемы таблицы переменных

### 🔧 Сервисный слой
- **VariablesService**: CRUD-операции с паттерном DI
- **Валидация**: Zod-схемы для операций создания/обновления
- **Обработка ошибок**: Кастомные классы ошибок с корректными HTTP-статусами

### 🛣️ Слой маршрутов
- **Express Router**: RESTful API-эндпоинты с middleware обработки ошибок
- **Паттерн DI**: Фабричные функции для внедрения зависимостей

## Установка

```bash
pnpm add @universo/flowise-variables-backend
```

## Использование

```typescript
import {
    createVariablesService,
    createVariablesRouter,
    Variable,
    variablesMigrations
} from '@universo/flowise-variables-backend'

// Создание сервиса с DI
const variablesService = createVariablesService({
    getDataSource: () => dataSource,
    telemetry: telemetryService,
    metrics: metricsProvider
})

// Создание роутера
const variablesRouter = createVariablesRouter({ variablesService })

// Подключение роутера
app.use('/api/v1/unik/:unikId/variables', variablesRouter)
```

## Экспорты

### База данных
- `Variable` - TypeORM-сущность
- `variablesMigrations` - Массив миграций

### Сервисы
- `createVariablesService` - Фабричная функция сервиса
- `IVariablesService` - Интерфейс сервиса
- `VariablesServiceConfig` - Тип конфигурации
- `VariablesServiceError` - Кастомный класс ошибки

### Маршруты
- `createVariablesRouter` - Фабричная функция роутера
- `variablesErrorHandler` - Express middleware обработки ошибок
- `VariablesControllerError` - Класс ошибки контроллера

## Структура файлов

```
packages/flowise-variables-backend/
├── base/
│   ├── src/
│   │   ├── database/
│   │   │   ├── entities/      # Сущность Variable
│   │   │   └── migrations/    # Миграции PostgreSQL
│   │   ├── services/          # Бизнес-логика
│   │   ├── routes/            # Express-роутер
│   │   └── index.ts           # Точка входа
│   ├── dist/                  # Скомпилированный код
│   ├── package.json
│   ├── tsconfig.json
│   ├── README.md              # Документация (EN)
│   └── README-RU.md           # Этот файл
└── package.json               # Конфигурация воркспейса
```

## API-эндпоинты

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/unik/:unikId/variables` | Получить все переменные для Unik |
| GET | `/unik/:unikId/variables/:id` | Получить конкретную переменную по ID |
| POST | `/unik/:unikId/variables` | Создать новую переменную |
| PUT | `/unik/:unikId/variables/:id` | Обновить существующую переменную |
| DELETE | `/unik/:unikId/variables/:id` | Удалить переменную |

## Разработка

```bash
# Установка зависимостей
pnpm install

# Сборка
pnpm build

# Запуск линтера
pnpm lint

# Режим отслеживания
pnpm dev
```

## Лицензия

Apache License Version 2.0 - См. файл [LICENSE](../../../LICENSE) для подробностей.
