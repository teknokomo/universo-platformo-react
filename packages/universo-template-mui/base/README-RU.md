# @universo/template-mui

> 🎨 Material-UI Dashboard шаблон для экосистемы Universo Platformo

## Информация о пакете

| Поле | Значение |
|------|----------|
| **Имя пакета** | `@universo/template-mui` |
| **Версия** | Смотрите `package.json` |
| **Тип** | React UI шаблон |
| **Сборка** | TypeScript с Material-UI v6 |
| **Назначение** | Современный и отзывчивый интерфейс панели управления |

## 🚀 Ключевые возможности

- 🎨 **Современный дизайн** - Material Design 3 с поддержкой темной/светлой темы
- 📱 **Отзывчивый макет** - Mobile-first отзывчивый дизайн
- 🌍 **Интернационализация** - Встроенная поддержка множественных языков
- 🧭 **Система навигации** - Динамические хлебные крошки и боковая навигация
- 🎯 **Настройка темы** - Обширные настройки темы MUI
- ⚡ **Типобезопасность** - Полная поддержка TypeScript
- 🔗 **Интеграция маршрутизации** - Полная интеграция с React Router v6

## Недавние изменения (2025-01-25)

### Обновления модернизации UI

- **Интеграция переключателя языков**: Добавлен компонент LanguageSwitcher в Header с иконками MUI v6 (заменяет иконки Tabler для лучшей совместимости)
- **Временно скрытые функции**: Поле поиска, выбор даты и колокольчик уведомлений закомментированы в Header для будущей реализации
- **Пункты меню агента/ассистента**: Временно скрыты из навигационного меню во время рефакторинга функциональности (URL остаются функциональными для тестирования)
- **Улучшенные хлебные крошки**: NavbarBreadcrumbs теперь принимает динамические элементы хлебных крошек с поддержкой интернационализации
- **Улучшенная навигация**: Улучшенное определение активного состояния меню для маршрутов панели управления

### Архитектурные улучшения

- **Типобезопасность**: Добавлены TypeScript интерфейсы для BreadcrumbItem и улучшенные свойства Header
- **Интернационализация**: Полная поддержка i18n для новых UI компонентов с резервными значениями по умолчанию
- **Модульный дизайн**: Компоненты разработаны для легкого повторного использования в разных разделах (uniks, metaverses, clusters)

## Описание

MUI Dashboard шаблон предоставляет комплексный интерфейс панели управления, построенный на Material-UI v6.

## Компоненты

### Основные компоненты макета

- **MainLayoutMUI**: Основная обертка макета с боковой панелью, заголовком и областью контента
- **Header**: Верхняя навигация с хлебными крошками, переключателем языков и переключателем темы
- **SideMenu**: Складываемая боковая панель с элементами навигации
- **NavbarBreadcrumbs**: Динамическая навигация хлебными крошками с поддержкой i18n

### Компоненты навигации

- **LanguageSwitcher**: Выпадающий список выбора языка с флаговыми значками
- **ColorModeIconDropdown**: Селектор режима темы (светлый/темный/системный)
- **MenuContent**: Динамический рендеринг содержимого меню с интеграцией React Router

### Недавние обновления компонентов

#### LanguageSwitcher (НОВЫЙ)
```tsx
// Новый компонент для переключения языков с иконками MUI v6
import LanguageSwitcher from '../shared/LanguageSwitcher';

// Использование в Header
<LanguageSwitcher />
```

#### Улучшенный NavbarBreadcrumbs
```tsx
// Теперь принимает динамические данные хлебных крошек
interface BreadcrumbItem {
  label: string;
  url?: string;
  isCurrentPage?: boolean;
}

<NavbarBreadcrumbs items={breadcrumbItems} />
```

## Возможности

### Интернационализация

- **Поддержка языков**: Английский и русский с расширяемой архитектурой
- **Динамическая загрузка**: Языки загружаются из ресурсов i18next
- **Обработка резервных вариантов**: Изящные резервы для отсутствующих переводов
- **Поддержка пространств имен**: Организованные ключи переводов по функциональным областям

### Система навигации

- **Динамические хлебные крошки**: Автоматически генерируются на основе текущего маршрута
- **Управление активным состоянием**: Правильное выделение текущей страницы в навигации
- **Конфигурация меню**: Централизованная конфигурация меню с легкой настройкой
- **Интеграция маршрутов**: Полная интеграция с React Router v6

### Система тем

- **Material Design 3**: Последние спецификации Material Design
- **Пользовательская палитра**: Фирменные цвета Universo Platformo
- **Переопределения компонентов**: Консистентный стиль всех компонентов
- **Отзывчивые точки останова**: Mobile-first отзывчивый дизайн

## Конфигурация

### Конфигурация меню

Элементы меню настраиваются в `src/navigation/menuConfigs.ts`:

```typescript
export const unikMenuConfig: MenuItemConfig[] = [
  {
    id: 'unik-dashboard',
    titleKey: 'menu.dashboard',
    urlPath: '',
    iconName: 'IconBuildingStore',
    type: 'item',
    breadcrumbs: false
  },
  // ... другие элементы меню
];
```

### Конфигурация хлебных крошек

Хлебные крошки автоматически генерируются в `MainRoutesMUI.tsx` на основе текущего маршрута:

```typescript
const generateBreadcrumbs = (pathname: string, unikId?: string, t?: any): BreadcrumbItem[] => {
  // Логика динамической генерации хлебных крошек
};
```

## Интеграция

### С Uniks Frontend

Шаблон интегрируется с `@universo/uniks-frt` через:

- **Route Factory**: `createMainRoutes()` принимает свойства компонента
- **Navigation Hook**: `useUnikNavigation()` предоставляет элементы меню
- **Ключи переводов**: Общее пространство имен переводов для согласованности

### Свойства компонента

```typescript
export interface MainLayoutMUIProps {
  disableCustomTheme?: boolean;
  mainItems?: Array<{ text: string; icon: React.ReactNode; url?: string; selected?: boolean }>;
  breadcrumbItems?: BreadcrumbItem[];
}
```

## Разработка

### Сборка

```bash
# Сборка пакета шаблона
pnpm --filter @universo/template-mui build

# Сборка с другими зависимыми пакетами
pnpm --filter flowise-ui build
```

### Тестирование навигации

Протестируйте систему навигации, посетив:

- Панель управления: `/unik/{id}` или `/unik/{id}/dashboard`
- Пространства: `/unik/{id}/spaces`
- Инструменты: `/unik/{id}/tools`
- Аналитика: `/unik/{id}/analytics`

### Добавление новых элементов меню

1. Добавьте элемент в `menuConfigs.ts`
2. Обновите ключи переводов в соответствующих файлах i18n
3. Добавьте маршрут в `MainRoutesMUI.tsx`
4. Протестируйте навигацию и активные состояния

## Временно скрытые функции

Эти функции временно скрыты, но URL остаются функциональными:

### Элементы меню (закомментированы в menuConfigs.ts)
- **Agent Flows**: `/unik/{id}/agentflows`
- **Assistants**: `/unik/{id}/assistants`

### Компоненты Header (закомментированы в Header.tsx)
- **Поле поиска**: Функциональность глобального поиска
- **Выбор даты**: Выбор диапазона дат
- **Уведомления**: Центр уведомлений со значком

## Будущие улучшения

- **Интеграция поиска**: Глобальный поиск по всем типам контента
- **Расширенные уведомления**: Система уведомлений в реальном времени
- **Фильтрация по дате**: Глобальная фильтрация по диапазону дат
- **Мобильные оптимизации**: Улучшенный мобильный навигационный опыт
- **Доступность**: Улучшения соответствия WCAG 2.1 AA

## Зависимости

### Основные зависимости

- `@mui/material`: Библиотека компонентов Material-UI
- `@mui/icons-material`: Иконки Material-UI
- `react-router-dom`: Клиентская маршрутизация
- `react-i18next`: Интернационализация
- `i18next`: Фреймворк переводов

### Зависимости разработки

- `typescript`: Проверка типов
- `@types/react`: Определения типов React
- Современные инструменты сборки для двойного вывода CJS/ESM

## Вклад в разработку

При вкладе в шаблон:

1. **Поддерживайте типобезопасность**: Все новые компоненты должны иметь правильные TypeScript интерфейсы
2. **Следуйте паттернам i18n**: Добавляйте ключи переводов с паритетом английского/русского
3. **Тестируйте отзывчивый дизайн**: Проверяйте работу компонентов на мобильных и настольных устройствах
4. **Документируйте изменения**: Обновляйте этот README со значительными модификациями
5. **Сохраняйте доступность**: Поддерживайте навигацию с клавиатуры и поддержку программ чтения с экрана

## Связанная документация

- [Документация основных приложений](../../../packages/README-RU.md)
- [Документация Uniks Frontend](../../uniks-frt/base/README-RU.md)
- [Архитектура платформы](../../../docs/ru/applications/README.md)

---

**Universo Platformo | MUI Dashboard шаблон**