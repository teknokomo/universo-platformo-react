# Компоненты чата Flowise (Legacy) 🏗️

> **⚠️ Уведомление о Legacy пакете**  
> Это **legacy пакет Flowise**, который будет **поэтапно выведен из использования и удален** в рамках модернизации архитектуры Universo Platformo. Функциональность чата рефакторится в новую, более масштабируемую архитектуру с использованием современных паттернов проектирования.
>
> **График миграции**: Этот пакет запланирован к удалению после Q2 2026, когда новая система чата будет полностью реализована.  
> **Новая архитектура**: Современные компоненты чата будут построены как самостоятельные пакеты `@universo/chat-*` с улучшенной поддержкой TypeScript, лучшей производительностью и более чистым API дизайном.

## Обзор

Legacy компоненты чат-сообщений для оригинальной платформы Flowise, предоставляющие интерфейс чата в реальном времени с потоковыми ответами ИИ, загрузкой файлов и записью голоса. Построен с Material-UI и Redux для управления состоянием.

## Текущие возможности

- **ChatMessage** - Основной компонент чата с потоковыми ответами ИИ
- **ChatPopUp** - Всплывающее окно чата  
- **ChatExpandDialog** - Полноэкранный диалог чата
- **Запись аудио** - Запись и воспроизведение голосовых сообщений
- **Загрузка файлов** - Поддержка прикрепления изображений и документов
- **История чата** - Навигация по истории ввода стрелками вверх/вниз
- **Поддержка потоков** - Потоковые ответы ИИ в реальном времени через Server-Sent Events
- **Рендеринг Markdown** - Форматирование богатого текста с поддержкой LaTeX математики

## Legacy архитектура

### Технологический стек
- **React 18+** с хуками и функциональными компонентами
- **Material-UI 5.15.0** для UI компонентов и тем
- **Redux + React-Redux** для управления глобальным состоянием  
- **Server-Sent Events** через `@microsoft/fetch-event-source` для потоков
- **React Markdown** с поддержкой LaTeX (rehype-mathjax, remark-math)
- **Запись аудио** через нативный MediaRecorder API

### Зависимости
- `@flowise/template-mui` - Legacy библиотека UI компонентов (также выводится из использования)
- `@flowise/store` - Legacy Redux store (заменяется)
- `@universo/api-client` - Современный API клиент (останется)
- `@universo/auth-frt` - Современная аутентификация (останется)
- `@universo/i18n` - Современная интернационализация (останется)

## Текущее использование (Legacy)

```javascript
import { ChatPopUp, ChatMessage, ChatExpandDialog } from '@flowise/chatmessage'

// Всплывающий чат
<ChatPopUp 
    canvasId={canvasId} 
    isAgentCanvas={false} 
    unikId={unikId}
    spaceId={spaceId} 
/>

// Встроенный компонент чата  
<ChatMessage
    canvasId={canvasId}
    isDialog={false}
    isAgentCanvas={false}
    previews={previews}
    setPreviews={setPreviews}
/>

// Полноэкранный диалог чата
<ChatExpandDialog
    show={showDialog}
    dialogProps={dialogProps}
    isAgentCanvas={false}
    onClear={handleClear}
    onCancel={handleCancel}
    previews={previews}
    setPreviews={setPreviews}
/>
```

## Справочник компонентов

### ChatPopUp
Плавающая кнопка действия, которая открывает всплывающий интерфейс чата.

**Ключевые Props:**
- `canvasId: string` - Идентификатор AI Canvas/Flow 
- `isAgentCanvas: boolean` - Определяет специфичное для агента поведение
- `unikId?: string` - Идентификатор рабочего пространства (опционально)
- `spaceId?: string` - Идентификатор пространства (опционально)

**Возможности:**
- Плавающая кнопка действия с иконкой чата
- Click-away listener для автозакрытия
- Позиционирование и переходы всплывающего окна
- Функциональность очистки истории чата
- Расширение до полноэкранного диалога

### ChatMessage  
Основной компонент интерфейса чата с полным UI беседы.

**Ключевые Props:**
- `canvasId: string` - Идентификатор AI Canvas/Flow
- `isDialog?: boolean` - Рендерится по-разному в диалогах vs встроенном режиме
- `isAgentCanvas: boolean` - Вариации UI специфичные для агента
- `previews: FilePreview[]` - Превью прикрепленных файлов
- `setPreviews: (previews: FilePreview[]) => void` - Сеттер состояния превью файлов

**Возможности:**
- История сообщений с аватарами пользователя/ИИ
- Потоковые ответы в реальном времени с индикаторами печати
- Загрузка файлов (изображения, документы) с превью
- Запись аудио с элементами управления воспроизведением  
- Функциональность копирования/скачивания сообщений
- Рендеринг LaTeX математики в сообщениях
- Навигация по истории ввода (стрелки вверх/вниз)
- Возможность регенерации ответа
- Поддержка аватаров агентов (режимы supervisor/worker)

### ChatExpandDialog
Обертка Material-UI Dialog для полноэкранного опыта чата.

**Ключевые Props:**
- `show: boolean` - Состояние видимости диалога
- `dialogProps: object` - Дополнительные props Material-UI Dialog
- `onClear: () => void` - Callback очистки истории чата
- `onCancel: () => void` - Callback закрытия диалога
- Все props ChatMessage передаются через компонент

**Возможности:**
- Полноэкранный отзывчивый диалог  
- Заголовок с действиями очистки и развертывания
- Встроенный компонент ChatMessage
- Правильное управление жизненным циклом диалога

## Legacy технические детали

### Управление состоянием (Redux)
```javascript
// Глобальное состояние чата хранится в @flowise/store
const chatState = {
    messages: [], // История сообщений чата
    loading: false, // Состояние потока
    error: null // Состояние ошибки
}
```

### Интеграция API
```javascript
// Использует @universo/api-client для HTTP запросов
// Server-Sent Events для потоковых ответов
import { fetchEventSource } from '@microsoft/fetch-event-source'

// Паттерн конечной точки потока
const streamUrl = `/api/v1/prediction/${canvasId}`
```

### Система загрузки файлов
```javascript
// Структура превью файла
interface FilePreview {
    data: string // Base64 данные
    type: string // MIME тип  
    name: string // Оригинальное имя файла
    size: number // Размер файла в байтах
}
```

### Запись аудио
```javascript
// Интеграция с нативным MediaRecorder
const mediaRecorder = new MediaRecorder(stream)
// Сохраняет в формате WebM/OGG
// Конвертирует в base64 для передачи API
```

## Стратегия миграции

### Этап 1: Анализ и планирование (Q1 2026)
- [ ] **Аудит текущего использования**: Составить карту всех использований компонентов ChatMessage в кодовой базе
- [ ] **Картирование зависимостей API**: Документировать все конечные точки API, связанные с чатом
- [ ] **Анализ управления состоянием**: Определить зависимости состояния Redux
- [ ] **Требования к функциям**: Определить требования к новой системе чата

### Этап 2: Дизайн современной архитектуры (Q1 2026)  
- [ ] **Новая структура пакетов**: Спроектировать архитектуру пакетов `@universo/chat-*`
- [ ] **TypeScript-First**: Определить строгие интерфейсы и типы
- [ ] **Управление состоянием**: Заменить Redux на React Context + useReducer
- [ ] **API слой**: Спроектировать новый chat API с лучшей обработкой ошибок
- [ ] **API компонентов**: Спроектировать более чистые, более композитные интерфейсы компонентов

### Этап 3: Реализация (Q2 2026)
- [ ] **Основной движок чата**: Построить новый потоковый движок чата
- [ ] **UI компоненты**: Реализовать современные компоненты чата с лучшей доступностью
- [ ] **Загрузка файлов**: Переработать обработку файлов с лучшей производительностью
- [ ] **Аудио система**: Реализовать новую запись аудио с опциями форматов

### Этап 4: Миграция и очистка (Q2 2026)
- [ ] **Постепенная миграция**: Заменить использование ChatMessage по одному модулю за раз
- [ ] **Тестирование**: Комплексное тестирование нового vs старого поведения  
- [ ] **Документация**: Полное руководство по миграции и документация нового API
- [ ] **Удаление пакета**: Удалить @flowise/chatmessage из кодовой базы

## Разработка (текущая)

### Предварительные требования
- Node.js 18+
- Среда рабочего пространства PNPM
- Доступ к legacy зависимостям Flowise

### Команды
```bash
# Собрать пакет (двойной вывод CJS/ESM)
pnpm --filter @flowise/chatmessage build

# Режим разработки с отслеживанием
pnpm --filter @flowise/chatmessage dev

# Очистить артефакты сборки
pnpm --filter @flowise/chatmessage clean
```

### Структура файлов
```
src/
├── components/
│   ├── ChatMessage.jsx        # Основной компонент чата (2659 строк)
│   ├── ChatPopUp.jsx          # Обертка всплывающего окна (248 строк)  
│   ├── ChatExpandDialog.jsx   # Обертка диалога
│   ├── ChatInputHistory.js    # Утилита истории ввода
│   ├── audio-recording.js     # Логика записи аудио
│   ├── ChatMessage.css        # Стили специфичные для чата
│   └── audio-recording.css    # Стили UI аудио
└── index.js                   # Экспорты пакета
```

## Известные проблемы и ограничения

### Проблемы производительности
- **Большая история сообщений**: Производительность ухудшается с 100+ сообщениями
- **Память загрузки файлов**: Большие файлы могут вызывать скачки памяти
- **Раздувание Redux Store**: Состояние чата растет без очистки

### Технический долг
- **Legacy код**: Смешанный JavaScript/JSX без TypeScript
- **Монолитные компоненты**: ChatMessage.jsx составляет 2659 строк (слишком большой)
- **Встроенные стили**: Смесь CSS файлов и sx props создает несогласованность
- **Обработка ошибок**: Недостаточные границы ошибок и восстановление

### Совместимость браузеров
- **Запись аудио**: MediaRecorder не поддерживается в старых браузерах
- **Загрузка файлов**: Обработка больших файлов варьируется по браузерам
- **Потоки**: Server-Sent Events требуют поддержки современного браузера

## Связанная документация

- [Flowise Store (Legacy)](../flowise-store/README-RU.md)
- [Flowise Template MUI (Legacy)](../flowise-template-mui/README-RU.md)
- [API Client (Modern)](../universo-api-client/README-RU.md)
- [Authentication (Modern)](../auth-frt/base/README-RU.md)
- [Дорожная карта архитектуры](../../docs/ru/roadmap/README.md)

---

**Universo Platformo | Legacy компоненты чата**  
*⚠️ Запланированы к замене современными пакетами @universo/chat-**