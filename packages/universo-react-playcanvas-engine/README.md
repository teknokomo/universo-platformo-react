# @universo-react/playcanvas-engine

Workspace wrapper for `playcanvas@2.18.1`.

The package intentionally keeps a thin entry point and re-exports the public PlayCanvas engine API:

```ts
export * from 'playcanvas'
```

It is seeded into the metahub package registry as a client/browser-targeted package.
